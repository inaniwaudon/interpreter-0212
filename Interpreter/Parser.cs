namespace Interpleter;

public class Parser
{
    private readonly List<Token> _tokens;

    public Parser(List<Token> tokens)
    {
        _tokens = tokens;
    }

    /*public Expr Parse()
    {
        /*for (int i = 0; i < _tokens.Count; i++)
        {
            _tokens[i].Type is "number")
        }
        return null;*/
    //}
}
